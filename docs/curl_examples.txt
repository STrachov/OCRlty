curl -X POST "http://0.0.0.0:8000/v1/health" 

curl -X POST "http://127.0.0.1:8001/v1/tilt/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "What is the total amount on this document? Answer only the number.",
    "temperature": 0.0,
    "max_tokens": 8,
    "pages": [
      {
        "ocr": {
          "width": 1000,
          "height": 1400,
          "words": [
            { "text": "Total",    "bbox": [100, 200, 220, 230] },
            { "text": "56",       "bbox": [230, 200, 340, 230] },
            { "text": "VAT",      "bbox": [100, 260, 170, 290] },
            { "text": "20%",      "bbox": [180, 260, 230, 290] }
          ]
        }
      }
    ]
  }'

curl -X POST "http://127.0.0.1:8000/v1/extract" \
  -H "Accept: application/json" \
  -F "file=@/workspace/src/tests/cord_train/receipt_00491.png;type=image/png" \
  -F 'question=You see the OCR words and positions from a sales receipt.

Find the line with the cash and read the amount written next to it.

Answer with that amount exactly as it is printed on the receipt, including any minus sign and currency symbol, and nothing else.'


________________________________
You see the OCR words and positions from a sales receipt.

Find all line items (products) that the customer bought.
On this type of receipt they are in the table above the summary lines
like "Total", "Cash", "Change".

For each product, read:
- the quantity,
- the item name,
- the line price on the right.

For example:  
1 Double Espresso     8,0
means: quantity = 1, item name = Double Espresso, price=8,0

List all item names from the receipt'


You see the OCR words and positions from a sales receipt.

Find the line with the cash and read the amount written next to it.

Answer with that amount exactly as it is printed on the receipt, including any minus sign and currency symbol, and nothing else.



________________________
You see the OCR words and positions from a sales receipt.

Find all line items (products) that the customer bought.
On this type of receipt they are in the table under the header
"Qty  Item Description  Price" and above the summary lines
like "Items Sold", "Sub Total", "Discount Total", "Total", "Cash", "Change".

For each product, read:
- the quantity,
- the item name,
- the line price on the right.

Answer with one line per product in this exact format:

QTY  ITEM_NAME  PRICE

Use the text exactly as it is printed on the receipt.
Do not include any other text before or after the list.


_________________________
python scripts/eval_cord_field.py \
  --field total_price \
  --prompt "You see the OCR words and positions from a sales receipt.

Find the final total amount the customer must pay (after all discounts and taxes).
Answer with that amount exactly as it is printed on the receipt, including currency symbol if any, and nothing else."

_________________________

You see the OCR words and positions from a sales receipt.\n\nFind the final total amount the customer must pay (after all discounts and taxes).
Answer with that amount exactly as it is printed on the receipt, including currency symbol if any, and nothing else.
____________
Pick the total amount (may be labeled with word grand or using abbreviations like ttl).
Return EXACTLY ONE of these strings (copy-paste, no edits):
[43.636, 100.000, 48.000, 52.000]
Output only the chosen string.

You see the OCR words and positions from a sales receipt. 
Find the line with the total amount (may be labeled with word grand or using abbreviations like ttl) and return the amount written right to it.
______________
Pick the TOTAL amount.
Return EXACTLY ONE of these strings (copy-paste, no edits):
[36.000, Rp51.000, 15.000, Rp0.000]
Output only the chosen string.
___________________
Pick ONLY the product/service line items (purchased items) from this receipt.
Exclude totals and payment lines such as: SUBTOTAL, TOTAL/TL, TAX, CASH, CHANGE/CG, etc.

Return ONLY elements FROM THIS ARRAY (copy characters exactly, no edits).
Output each chosen element on a new line. Output nothing else.

[
"01|CHO MOUSSE .20,000",
"02|GRAPE JELLY .13,000",
"03|SUBTOTAL .30,000",
"04|TAX 10% .3,000",
"05|TL 33,000",
"06|CASH .50,000",
"07|CG .17,000"
]
___________________

curl -X POST "http://127.0.0.1:8000/v1/extract" \
-H "Accept: application/json" \
-F "file=@/workspace/src/data/cord_subset/images/cord_0050.jpg;type=image/png" \
-F "field_name=total_price" \
-F "max_neighbours=3" \
-F 'question=Pick the TOTAL amount.
Return EXACTLY ONE of these strings (copy-paste, no edits):
{{}}
Output only the chosen string.'
